version: 0.2
 
phases:
  pre_build:
    commands:
      - aws --version
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 484537496937.dkr.ecr.us-east-1.amazonaws.com
      
  build:
    commands:
      - docker build -t hello-world .  # Replace 'your-image-name' with your desired image name
      - docker tag hello-world:latest public.ecr.aws/v9g5c6v3/hello-world:latest  # Replace 'your-repo-name' with your ECR repository name
  post_build:
    commands:
      - docker push public.ecr.aws/v9g5c6v3/hello-world:latest

